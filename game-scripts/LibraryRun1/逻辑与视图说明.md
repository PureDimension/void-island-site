# LibraryRun1 逻辑与视图说明

这份文档用于用中文概括当前 `logic.js`、`view.jsx` 以及它们依赖的数据与运行时逻辑。

补充约定：
- 如果没有额外指令，不主动修改前端展示用的单位规则描述文字。
- 所有改动都限制在 `game-scripts/LibraryRun1` 内。
- 当前重构前的旧版本已备份在 `/_backup_2026-03-29_pre_refactor/`。

## 1. 文件结构

### 入口
- `logic.js`
  只负责导出运行时。
- `engine/runtime.js`
  只是薄包装，真正逻辑在 `engine/runtime-core.js`。
- `view.jsx`
  负责剧情页、战斗页、箭头、教程卡片、右侧面板、LOG/RULES、失败与结算弹层。

### 后端运行时
- `engine/runtime-core.js`
  负责状态初始化、剧情切换、战斗结算、确认流、BUFF 生命周期、回退、胜负判定。
- `engine/enemy-ai.js`
  负责按关卡注册的敌方 AI 选择行动。

### 前端视图模型
- `engine/view-model.js`
  负责把后端状态整理成前端直接渲染的数据，例如：
  - BUFF 简述
  - POWER 右侧 `+x`
  - 中央简略 log
  - 单位面板里的动态上下文条目
  - 单位是否可被玩家操控、可否被选为目标

### 数据层
- `data/stories.js`
  存剧情文本。
- `data/flow.js`
  存整局流程顺序。
- `data/rules.js`
  存 RULES 面板文本。
- `data/guides.js`
  存教程关、第一关、第二关的引导步骤。
- `data/stages/*.js`
  存每一关的标题、目标、敌方伪代码、AI id、阵容与初始日志。
- `data/buffs.js`
  存 BUFF 注册表。
- `data/units/catalog.js`
  存单位定义、后端 hook、前端 inspector hook。

## 2. 流程结构

当前流程由 `data/flow.js` 控制，顺序为：

1. 教程关前剧情
2. 教程关战斗
3. 教程关后剧情
4. 第一关前剧情
5. 第一关战斗
6. 第二关前剧情
7. 第二关战斗

`runtime-core.js` 的 `moveToFlow()` 负责在剧情与战斗之间切换。

## 3. 顶层状态

### 顶层字段
- `phase`
  当前所处阶段：`STORY` / `BATTLE` / `DEFEAT` / `VICTORY`
- `flowIndex`
  当前位于流程链的哪一段
- `story`
  剧情状态
- `battle`
  战斗状态
- `campaign`
  整局持久信息，目前主要包括：
  - `overloadUsed`
  - `clearedStages`
  - `ratings`
- `history`
  回退快照
- `rulebook`
  当前已解锁的规则文本
- `pendingDefeat`
  暂挂失败状态
- `finalResults`
  最终结算

### 剧情状态
- `sceneId`
- `index`
- `pages`

前端表现为：
- 每次点击出现一段
- 全段出现后才进入下一页
- 提示文字在右下角

### 战斗状态
- `stageId`
- `title`
- `objective`
- `enemyLogicText`
- `enemyAi`
- `stageRuntime`
- `status`
- `turn`
- `destroyedUnitIds`
- `pendingTurnEffects`
- `pendingEnemyAction`
- `lastEnemyAttackerId`
- `lastPlayerAttackerId`
- `lastCombatAttackerId`
- `lastCombatDefenderId`
- `playerUnits`
- `enemyUnits`
- `actionLog`
- `guide`

## 4. 单位与能力架构

### 单位实例
战斗中的单位实例包含：
- `code`
  单位原始模板 id
- `abilityCode`
  当前实际能力模板 id
- `side`
  当前阵营语义
- `baseSide`
  初始阵营，用于前端分排显示
- `power`
- `basePower`
- `description`
- `tags`
- `display`
- `buffs`
- `combat`
- `runtimeState`

### 单位模板
`data/units/catalog.js` 中每个单位模板定义包含：
- `name`
- `power`
- `description`
- `tags`
- `display`
- `hooks`
- `viewHooks`
- `runtimeState`

### 后端 hook 时点
当前统一支持：
- `beforeCombat`
  战斗开始前触发
- `modifyCombatPower`
  计算本次战斗 POWER 时触发
- `preventCombatDestruction`
  某单位将因战斗被摧毁时触发，可阻止这次摧毁
- `afterCombat`
  战斗流程结算完成后触发
- `onDestroyed`
  单位被摧毁时触发
- `previewCombatBonus`
  给前端 POWER 右侧的 `+x`

### 前端显示 hook
当前使用 `viewHooks.inspectorEntries` 输出：
- `{ key, value }`

右侧信息面板会直接渲染这些条目，因此像“上一个攻击者是谁”这类动态信息不再写死在 `view.jsx`。

补充：
- 影子的 `显现` 现在允许在真正发动攻击前取消
- 前端做法是不新增文案，而是再次点击当前已选的显现数值时直接清空待使用值

## 5. BUFF 体系

当前 BUFF 定义在 `data/buffs.js`：

- `【电磁干扰】`
  获得时立刻 `POWER -3`；带着它的单位被摧毁时，最后一次与其战斗的单位会继承它。
- `【抗体】`
  本身没有额外数值效果，但会影响第二关若干单位的目标判定。
- `【标记】`
  本身没有额外数值效果，但会影响第二关若干单位的目标判定。
  全场同一时刻至多存在一个，新的【标记】会挤掉旧的【标记】。
- `【病毒】`
  会持续到下一个回合结束前。
  在此期间该单位临时改变阵营，能力中的“己方 / 敌方”判定都按新阵营解释。
  它不可被原敌对阵营选为攻击目标，但可被新阵营选为攻击者。

### 回合结束清理
`timing = "turn-end"` 的 BUFF 会在敌方回合结算完成、即将回到下一次玩家回合时清理。
目前就是 `【病毒】`。

## 6. 战斗结算

### 普通战斗流程
`settleCombatOutcome()` 的顺序是：

1. 记录本次参战双方最近一次战斗对象
2. 读取“上一战攻击者 / 被攻击者”
3. 触发 `beforeCombat`
4. 计算双方本次战斗 POWER
5. 写入 `combat` 日志
6. 判定谁会被摧毁
7. 触发“免于因战斗被摧毁”
8. 执行真实摧毁
9. 更新击杀数
10. 更新 `lastCombatAttackerId / lastCombatDefenderId`
11. 触发 `afterCombat`
12. 对全场存活单位做一次 POWER 越界检查

### 敌方确认流
普通敌方行动不是在敌方出手瞬间立刻结算，而是：

1. 先生成 `pendingEnemyAction`
2. 前端显示箭头与 `CONFIRM`
3. 玩家确认
4. 后端才真正执行战斗与 hook

这样可以保证：
- 未确认前不会提前扣 `POWER`
- 未确认前不会提前死亡
- 未确认前不会提前触发战后 hook

### B 细胞连击确认流
现在 `gateway-b-cell` 已经从“一次性整包结算”改成了真正的分段状态机：

1. 玩家或敌方指定 `B 细胞` 的攻击目标
2. 先生成一个 `gateway-sequence / announce` 待确认动作
3. 这一步前端显示 `B 细胞 -> 目标` 的虚线箭头
4. 点击 `CONFIRM` 后，才真正触发 `B 细胞` 自身的 `beforeCombat`
5. 然后依次生成每一个参战小兵的待确认动作
6. 每个小兵各自显示一次实线箭头，并各自要求一次 `CONFIRM`
7. 全部小兵攻击结束后，才统一触发 `B 细胞` 的 `afterCombat`

因此：
- 发动几次支援攻击，就暂停几次要求确认
- `B 细胞` 本体虚线只表示调度
- 真正造成战斗结算的是后续实线小兵攻击

## 7. 第二关相关规则

### 目标选择
第二关的巨噬细胞、B 细胞、T 细胞可以按设计稿去选取不限定敌我的目标，但仍然要服从：
- 单位本身必须 `combatTargetable !== false`
- 带 `【病毒】` 的单位不能被其原敌对阵营攻击

### 顺位机制
当前顺位判定分成两层：
- 若调用方没有提供参照阵营，则仍按稳定顺序排序，保证 hook 执行顺序可复现
- 若调用方提供了参照阵营或参照单位，则“敌方优先”会改为相对该参照对象判断

也就是说：
- 优先的是“相对于自己或当前攻击者的敌方”
- 不是固定写死成电脑方优先
- 同阵营内仍按 `slot` 从小到大决定顺位

### 巨噬细胞
- 出手前先给己方中 `POWER` 最低且没有任何 BUFF 的单位挂【抗体】
- 再在合法目标里找另一个没有【标记】、没有【抗体】、且 `POWER` 最低的单位
- 战斗前吸取目标 `2` 点 `POWER`

### B 细胞
- 出手前先给己方中 `POWER` 最高且没有任何 BUFF 的单位挂【抗体】
- 仅当自己作为攻击者时，才不直接进行普通战斗
- 此时改为调度当前阵营下所有存活的“巡检单核体 / 清理溶酶虫”依次攻击目标
- 若自己作为被攻击者，则按自身 `POWER` 正常参与战斗
- 行动结束后复活自己原始阵营里的相关小兵

### 小兵复活与位置
- 复活时会按当前控制方切换 `side`
- 但不会再把单位从原数组挪到另一排
- 前端分排改为按 `baseSide` 渲染

因此现在：
- 被复活成玩家方的小兵仍然显示在上排
- 但其当前阵营语义已经是玩家方
- 它可以被玩家选作攻击者

## 8. 拼图修正

`拼图` 现在修正为：
- 如果本次战斗开始时是两块拼图，则战后失去一块拼图并 `POWER -3`
- 这一场战斗里一旦触发“碎一块”，就立即结束本单位本次 `afterCombat`
- 不会在同一场战斗里又碎又补

因为 `B 细胞` 连击已经被拆成多次独立战斗，所以：
- 每个小兵攻击都被视作单独一场战斗
- `拼图` 的失去 / 恢复也会按每一段独立判断

## 9. 前端视图结构

### 战场布局
- 上排：`baseSide === "enemy"` 的单位
- 下排：`baseSide === "player"` 的单位
- 中间：简略 log 与箭头层
- 右侧：敌方伪代码 + 单位详细信息 + 操作按钮

### 选中与颜色
- 可作攻击者：浅蓝
- 选中的攻击者：亮蓝
- 可作目标：浅橙
- 选中的目标：亮橙
- `伪装模块` 使用上下分区分别承载目标逻辑与攻击者逻辑

### 确认阶段高亮
确认阶段不再只有一种颜色：
- 当前控制方攻击者会用其阵营色高亮
- 当前被攻击方会用相反阵营色高亮

因此：
- 敌方攻击玩家时：敌方橙色、玩家蓝色
- 玩家操控感染单位时：玩家蓝色、目标橙色

### 箭头
- 普通攻击：实线
- `B 细胞` 调度线：虚线
- `B 细胞` 支援小兵攻击：实线

### 右侧面板 4:6
右侧面板现在改成三段式：
- 上部 `4`
  敌方伪代码
- 中部 `6`
  单位详情、描述、上下文、BUFF、主动技能
- 下部
  `ATTACK / CONFIRM`

如果伪代码过长：
- 伪代码区域自身滚动

## 10. 前端与音乐播放器

`view.jsx` 进入游戏时会：

1. 给 `body` 挂 `library-run-hide-player`
2. 监听 DOM 变化
3. 隐藏 layout 里的全局悬浮音乐播放器
4. 离开时恢复

## 13. 追加修正：右上角音乐控件

这轮新增了一个独立于站点全局播放器的本地音乐控件。

实现方式：
- 继续隐藏 layout 里的全局悬浮播放器
- 在 `LibraryRun1` 视图内部挂一个独立 `audio` 元素
- 右上角新增 `BGM` 小按钮
- 进入页面时立即尝试播放当前阶段对应的音频
- 如果浏览器拦截自动播放，则在首次点击或按键后再次尝试播放

当前曲目映射放在 `data/music.js`：
- 教程关及之前：借用自《ever17》，曲目 `Kosmisher Wal`
- 第一关战斗：借用自《密室的祭品》，曲目 `Truth`
- 剧情段：借用自《素晴日》，曲目 `Tractatus Logico-philosophicus`
- 第二关战斗：借用自《废墟图书馆》，曲目 `KetherBattle02`

补充：
- 教程前剧情、教程战斗、教程后剧情、第一关前剧情这几段现在统一继续使用教程曲
- 这样进入第一关战斗前不会因为多次切阶段而反复重新加载音频
- 从第二关前剧情开始，剧情段再切到《素晴日》
- `view.jsx` 里的 `audio` 元素现在已经提升到 `LibraryRun1View` 顶层统一挂载
- 剧情页与战斗页切换时只切内容，不再切换或重建 `audio` 节点
- 因此当相邻阶段解析到的是同一首曲目时，会直接沿用当前播放实例继续播放，不应再出现“教程关以前 / 教程关 / 教程关以后”之间的自动暂停

音频文件本体已整理到 `audio/` 目录，并统一改成纯 ASCII 文件名：
- `audio/tutorial.mp3`
- `audio/story.mp3`
- `audio/stage1.mp3`
- `audio/stage2.mp3`

这些文件不会直接通过 `game-scripts/...` 静态暴露。
当前做法是新增一个很窄的 API 路由：
- `app/api/library-run1-audio/[track]/route.jsx`

它只允许读取：
- `tutorial`
- `story`
- `stage1`
- `stage2`

并从 `game-scripts/LibraryRun1/audio/` 中返回对应 mp3。

因此游戏内播放器现在请求的是：
- `/api/library-run1-audio/tutorial`
- `/api/library-run1-audio/story`
- `/api/library-run1-audio/stage1`
- `/api/library-run1-audio/stage2`

这样既避免了中文文件名、空格和特殊字符在静态资源路径中造成 404，也不需要去改敏感的全局转发逻辑。

## 14. 追加修正：关前剧情

这轮重写了三段关前剧情：
- `tutorial-pre`
- `stage1-pre`
- `stage2-pre`

叙事重点放在：
- 影子为何会行动
- 影子与水铃儿、A 之间的连接
- 影子第一次见到图书馆时的执念
- A 追寻 A 的姐姐与图书馆的理由
- 第二关前萤草正式接入并提供帮助

整体上严格跟随 `剧情设计.md` 结尾已有的大纲，不再额外给 A、B、A 的姐姐命名，也尽量减少自行发挥的背景扩写，把重点压回“影子的故事”本身。

前端表现：
- 右上角 `BGM` 按钮已改为播放 / 暂停图标，可手动暂停 / 恢复
- 曲目切换并成功开始播放时，会在页面上方显示一个明显的提示框
- 提示框停留约 `10s`
- 提示框也支持点击后立即隐藏
- 提示框中显示借用来源、音频名称与作者
- 教程卡片自身不再使用毛玻璃模糊，并单独补了文字清晰渲染设置；这次调整只针对教程文字清晰度，不影响战场其他文字
- 右侧栏的两个滚动区 `敌方伪代码` 与 `单位详情` 现在使用了独立定制滚动条，颜色分别偏橙与偏蓝，以保持与整体科技风一致

## 11. 当前这轮修改的重点

这轮主要完成了以下修正：

- 修正了 `拼图` 在同一场战斗里既失去拼图又恢复拼图的问题
- 把 `B 细胞` 攻击改成了虚线宣告 + 多次实线支援攻击 + 多次 `CONFIRM`
- 让被复活成玩家方的小兵仍然留在上排显示
- 把 `【病毒】` 统一解释成“临时改变阵营”，并禁止被原敌对阵营攻击
- 把右侧伪代码与单位详情的视觉比例改成 `4:6`
- 让伪代码区域在过长时独立滚动

## 12. 追加修正：清理溶酶虫

这一轮额外修正了第二关里 `清理溶酶虫` 的结算时点问题。

之前的错误实现是：
- 先触发 `清理溶酶虫` 的技能效果
- 然后仍继续进行一次普通战斗对撞

这会导致一种错误现象：
- 例如目标原本 `POWER 5`
- 被 `清理溶酶虫` 攻击后先变成 `POWER 2`
- 随后又与 `POWER 2` 的 `清理溶酶虫` 继续普通对撞
- 结果双方同归于尽

现在的正确实现是：
- `清理溶酶虫` 的技能效果本身就是这次攻击的完整结算
- 若目标 `POWER > 3`，则仅令目标 `POWER -3`
- 否则，直接摧毁目标
- 技能执行完后，不再继续进行普通战斗对撞

因此像“杀手T细胞-协议 被清理溶酶虫攻击”这种场景里：
- 若其当前 `POWER 5`
- 结果应为变成 `POWER 2`
- 不应再因为后续普通对撞而被额外摧毁
